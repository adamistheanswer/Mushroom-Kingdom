# Voice chat needs a TURN relay, or it fails silently for players behind symmetric NAT
# (most mobile networks). dash.cloudflare.com -> Realtime -> TURN -> create a TURN Server app
TURN_TOKEN_ID=
TURN_API_TOKEN=
